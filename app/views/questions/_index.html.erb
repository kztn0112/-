<% case params[:type] %>
<% when 'making' then %>
  <p>making</p>
<% when 'bread' then %>
  <p>Bread</p>
<% else %>
  <p>全質問</p>
<% end %>

<% if questions.count == 0 %>
~~~~~~
<% end %>

<% questions.each do |question| %>
  <span><%= link_to question.title, question_path(question)  %></span>
  <%= attachment_image_tag question, :image, :fill, 100, 100, format: 'jpeg' %>
   <p><%= "#{question.answers.count} コメント" %></p>

<% end %>
